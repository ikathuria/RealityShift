# Legacy Prototype Archive

This directory preserves the original RealityShift prototype and hackathon materials.

It exists for reference only while the browser-first rebuild moves into:

- `packages/game-client/`
- `worker/`

The contents here should not be treated as the production direction for the new version of the game.

